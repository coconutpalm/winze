<configuration debug="false">
  <!-- Log file in the data directory -->
  <property name="LOG_DIR" value="${user.home}/.local/share/winze"/>

  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/plan-server.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/plan-server.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      <maxFileSize>10MB</maxFileSize>
      <maxHistory>7</maxHistory>
      <totalSizeCap>50MB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Quiet noisy libraries -->
  <logger name="org.lmdbjava" level="WARN"/>
  <logger name="datalevin" level="INFO"/>
  <logger name="nrepl" level="INFO"/>

  <root level="INFO">
    <appender-ref ref="FILE"/>
  </root>
</configuration>
